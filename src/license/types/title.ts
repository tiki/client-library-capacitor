/*
 * Copyright (c) TIKI Inc.
 * MIT license. See LICENSE file in root directory.
 */

export interface Title {
    id: string
    hashedPtr: string
    tags: string[]
    timestamp: string
    description: string
  }